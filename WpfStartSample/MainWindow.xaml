<Window x:Class="WpfStartSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfStartSample"
        mc:Ignorable="d"
        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
        BorderThickness="1" ResizeMode="CanResizeWithGrip" WindowStyle="None"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <Grid Height="45" Background="#FF4E4E4E">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>

            <materialDesign:PackIcon Kind="MaterialDesign" Grid.Column="0" Foreground="White" Height="35" Width="35" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <!--<Image Grid.Column="0" Width="32" Height="50" Source="/TowerLogo.png"/>-->

            <Canvas Grid.Column="1">
                <Label Content="TUMUGU" FontSize="22" FontFamily="Arial Black" Foreground="White" Height="35" Margin="0,1,0,0"/>
                <Label Content="- Markdown Editor -" FontSize="9" FontFamily="Arial Black" Foreground="WhiteSmoke" HorizontalAlignment="Left" VerticalAlignment="Bottom" Height="21" Margin="0,25,0,0"/>
            </Canvas>

            <Button Grid.Column="3" x:Name="BtnClose" Style="{DynamicResource MaterialDesignToolButton}" Height="40" Width="40" Click="BtnClose_Click" ToolTip="Close" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0.2,5,-0.4,5">
                <materialDesign:PackIcon Kind="Close" Foreground="White" Height="35" Width="35" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>
        </Grid>

        <Grid Height="383">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Markdownの入力TextBox -->
            <TextBox Grid.Row="0" Grid.Column="0" x:Name="InputTextBox" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" TextWrapping="Wrap" FontSize="16" FontFamily="Consolas" Margin="5"/>

            <!-- TextBoxとWebView2の中央に左右の画面サイズ変更のため配置 -->
            <GridSplitter Grid.Row="1" Grid.Column="1" Width="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="LightGray" ShowsPreview="True"/>

        </Grid>


    </StackPanel>
    
</Window>
